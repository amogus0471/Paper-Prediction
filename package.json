{
  "name": "paper-predictions",
  "private": true,
  "version": "0.1.0",
  "description": "Paper-trade real prediction markets against the real order book. Chrome MV3 extension. No real money, ever.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint .",
    "format": "prettier --write .",
    "check:secrets": "node scripts/check-secrets.mjs",
    "invariants": "npm test --workspace @polyfill/venues -- test/live-invariants.test.ts",
    "build:edge": "node scripts/build-edge-shared.mjs",
    "build:functions": "node scripts/build-functions.mjs",
    "verify": "npm run check:secrets && npm run typecheck --workspaces --if-present && npm test --workspaces --if-present && npm run build --workspace @polyfill/extension",
    "pack": "npm run build --workspace @polyfill/extension && npm run pack --workspace @polyfill/extension"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@vitest/coverage-v8": "^2.1.8",
    "esbuild": "^0.28.1",
    "fast-check": "^3.23.2",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "license": "MIT"
}
